openapi: 3.0.3
info:
  title: "AUTHZ-003 API"
  version: "1.0.0"

paths:
  /api/v1/projects/{project_id}:
    get:
      summary: Get Project
      parameters:
        - name: project_id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: OK

