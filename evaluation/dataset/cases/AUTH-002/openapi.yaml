openapi: 3.0.3
info:
  title: "AUTH-002 API"
  version: "1.0.0"
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer

paths:
  /api/v1/admin/users:
    get:
      summary: Get Admin Users
      security:
        - bearerAuth: []
      responses:
        '200':
          description: OK

