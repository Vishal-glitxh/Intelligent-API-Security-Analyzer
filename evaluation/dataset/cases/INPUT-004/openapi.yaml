openapi: 3.0.3
info:
  title: "INPUT-004 API"
  version: "1.0.0"

paths:
  /api/v1/comments:
    post:
      summary: Add Comment
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                text:
                  type: string
                  maxLength: 500
      responses:
        '201':
          description: Created

